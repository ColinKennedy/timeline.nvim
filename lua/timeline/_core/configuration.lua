local M = {}

M._DEFAULTS = {
    repository_paths = {"~/.vim_custom_backups"},
    sources = {
    },
    -- TODO: Add window defaults (width [percentage, fixed width, etc])
}

M.DATA = {}

return M
